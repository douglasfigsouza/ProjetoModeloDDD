@model IEnumerable<ProjetoModeloDDD.Mvc.ViewModels.ClienteViewModel>

@{
    ViewBag.Title = "Especiais";
}

<h2>Especiais</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nomeCli)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.sobrenomeCli)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ativoCli)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.emailCli)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nomeCli)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.sobrenomeCli)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ativoCli)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.emailCli)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.clienteId }) |
            @Html.ActionLink("Details", "Details", new { id=item.clienteId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.clienteId })
        </td>
    </tr>
}

</table>
